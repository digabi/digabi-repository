ask-passphrase

# Wait for 10 seconds * 3 times if lockfile exists
waitforlock 3

# Do not check free diskspace before downloads, assume that we have enough
#spacecheck none

# Configure paths
basedir /var/lib/digabi-repository
outdir +b/data/repository
logdir +b/data/logs
dbdir +b/data/db
listdir +b/data/lists
morguedir +o/morgue

