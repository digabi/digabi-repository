ask-passphrase

# Wait for 10 seconds * 3 times if lockfile exists
waitforlock 3

# Do not check free diskspace before downloads, assume that we have enough
#spacecheck none

# NOTE: For path configuration, see also /etc/defaults/digabi-repository
outdir +b/repository
dbdir +b/db
listdir +b/lists
morguedir +b/morgue

