#!/bin/sh

set -e

case "$1" in
    configure)
	adduser --system --home /var/lib/digabi-repository --shell /bin/sh --group digabi-repository || true
        ;;
    *)
        ;;
esac

#DEBHELPER#

exit 0
