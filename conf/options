ask-passphrase

# Wait for 10 seconds * 3 times if lockfile exists
waitforlock 3

# Do not check free diskspace before downloads, assume that we have enough
#spacecheck none

# Configure paths
basedir /var/lib/digabi-repository
outdir +b/repository
logdir +b/logs
dbdir +b/db
listdir +b/lists
morguedir +b/morgue

